<h1>New product</h1>
<% if current_user %>
	<%= render 'form' %>
	<% else %>
  		<p>Please <%= link_to "log in", new_sessions_path %> to add a product.</p> 
	<% end %>

<%= link_to 'Back', products_path %>